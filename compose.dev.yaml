services:
  backend:
    build: ./backend
    container_name: alphaforge-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: alphaforge-frontend
    ports:
      - "4200:80"
    env_file:
      - ./frontend/.env
    depends_on:
      - backend
    restart: unless-stopped